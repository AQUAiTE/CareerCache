import '@mantine/core/styles.css'
import { MantineProvider } from '@mantine/core' 

export default function Login() {
  return (
    <MantineProvider>
      Welcome back to CareerCache!
    </MantineProvider>
  )
}
